import { Text, View, Button, } from "react-native"
import { styles } from "../lib/style"
export const Local = ({navigation}) =>{
    return (
        <View style={styles.container}>

            <Text>Tela De Local</Text>
        </View>
    )
}